#include <stdio.h> //printf declarada en stdio
//preprocesado que lee el # para usar las funciones, su declaración, su nombre y cuántos argumentos acepta y su definición está en su librería, pero esto está en el segundo proceso

int main(){ //Se puede poner void explorar
//Podemos darle como argumento del terminal
	printf("Hola Mundo!\n"); //El \n es un caracter que indica nueva linea
}

//Para ejecutar se compila con gcc hola_mundo.c -o hola_mundo.o o .x, esto es para identificar el archivo ejecutable
